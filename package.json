{
  "name": "planner",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "bump-version": "node scripts/bump-version.js",
    "build": "npm run bump-version && npm run test"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "jsdom": "^24.0.0"
  }
}
